#### Stream set up
1. It starts a live stream of user data
2. start the server via `uvicorn stream:app --reload`
3. use `flowchart.json` as the base input json
4. you can generate more json with frontend object via `frontend2backend.js`